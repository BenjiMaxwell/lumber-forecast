{
  "name": "lumberflow",
  "version": "1.0.0",
  "description": "AI-Powered Lumber Inventory Forecasting System",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

